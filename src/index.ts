/**
 * `@supabase/middleware-openfeature` — typed OpenFeature flag resolution for
 * `@supabase/middleware`.
 *
 * @packageDocumentation
 */

/** Package name, exported so the scaffold has something to assert against. */
export const packageName = '@supabase/middleware-openfeature'
