# wrangler's workerd and esbuild fetch their binaries in a postinstall step,
# which pnpm blocks unless the package is allowed here.
allowBuilds:
  esbuild: set this to true or false
  workerd: set this to true or false
